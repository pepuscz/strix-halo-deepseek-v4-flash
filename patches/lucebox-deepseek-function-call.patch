diff --git a/server/src/server/http_server.cpp b/server/src/server/http_server.cpp
index 61484b8..de64e7f 100644
--- a/server/src/server/http_server.cpp
+++ b/server/src/server/http_server.cpp
@@ -620,7 +620,7 @@ json build_props_body(const ServerConfig & config,
     const bool is_qwen = (config.arch.rfind("qwen", 0) == 0);
     const bool reasoning_supported = is_qwen;
     const bool speculative_supported = is_qwen;
-    const bool tools_supported = is_qwen;
+    const bool tools_supported = is_qwen || config.arch == "deepseek4";
 
     auto pcs  = prefix_cache.stats();
     auto pcfs = prefix_cache.full_stats();
diff --git a/server/src/server/tool_parser.cpp b/server/src/server/tool_parser.cpp
index c7f53be..0f62d47 100644
--- a/server/src/server/tool_parser.cpp
+++ b/server/src/server/tool_parser.cpp
@@ -559,8 +559,12 @@ static bool parse_json_tool_call(const json & obj, std::string & out_name, json
                 return false;
             }
         }
-    } else if (obj.contains("function") && obj["function"].is_object()) {
-        const auto & fn = obj["function"];
+    } else if ((obj.contains("function") && obj["function"].is_object()) ||
+               (obj.contains("function_call") &&
+                obj["function_call"].is_object())) {
+        const auto & fn = obj.contains("function")
+            ? obj["function"]
+            : obj["function_call"];
         if (!fn.contains("name") || !fn["name"].is_string()) return false;
         name = fn["name"].get<std::string>();
         if (fn.contains("arguments")) {
diff --git a/server/test/test_server_unit.cpp b/server/test/test_server_unit.cpp
index fa4f678..0b23595 100644
--- a/server/test/test_server_unit.cpp
+++ b/server/test/test_server_unit.cpp
@@ -709,6 +709,21 @@ TEST_CASE(ServerUnitFixture, test_parse_json_tool_call) {
     }
 }
 
+TEST_CASE(ServerUnitFixture, test_parse_legacy_openai_function_call_wrapper) {
+    std::string text =
+        "{\"function_call\":{\"arguments\":"
+        "\"{\\\"location\\\":\\\"Prague\\\"}\","
+        "\"name\":\"get_weather\"}}";
+    auto result = parse_tool_calls(text, weather_tools());
+    TEST_ASSERT(result.tool_calls.size() == 1);
+    if (!result.tool_calls.empty()) {
+        TEST_ASSERT(result.tool_calls[0].name == "get_weather");
+        auto args = json::parse(result.tool_calls[0].arguments);
+        TEST_ASSERT(args["location"] == "Prague");
+    }
+    TEST_ASSERT(result.cleaned_text.empty());
+}
+
 TEST_CASE(ServerUnitFixture, test_parse_single_tool_bare_json_args) {
     std::string text =
         "{\n"
@@ -1342,6 +1357,26 @@ TEST_CASE(ServerUnitFixture, test_emitter_single_tool_bare_json_args) {
     TEST_ASSERT(em.accumulated_text().empty());
 }
 
+TEST_CASE(ServerUnitFixture, test_emitter_legacy_openai_function_call_wrapper) {
+    auto em = make_emitter(ApiFormat::OPENAI_CHAT, weather_tools());
+    em.emit_start();
+    em.emit_token("{\"function_call\":{\"arguments\":");
+    em.emit_token("\"{\\\"location\\\":\\\"Prague\\\"}\",");
+    em.emit_token("\"name\":\"get_weather\"}}");
+    const auto finish = em.emit_finish(16);
+
+    TEST_ASSERT(em.tool_calls().size() == 1);
+    if (!em.tool_calls().empty()) {
+        TEST_ASSERT(em.tool_calls()[0].name == "get_weather");
+        auto args = json::parse(em.tool_calls()[0].arguments);
+        TEST_ASSERT(args["location"] == "Prague");
+    }
+    TEST_ASSERT(em.accumulated_text().empty());
+    const std::string wire = concat(finish);
+    TEST_ASSERT(wire.find("get_weather") != std::string::npos);
+    TEST_ASSERT(wire.find("tool_calls") != std::string::npos);
+}
+
 TEST_CASE(ServerUnitFixture, test_emitter_bare_json_args_do_not_trigger_after_content) {
     auto em = make_emitter(ApiFormat::ANTHROPIC, shell_tools());
     em.emit_start();
