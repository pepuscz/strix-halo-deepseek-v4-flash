---
- name: Install the pinned DeepSeek V4 Flash production release
  hosts: deepseek_hosts
  become: true
  gather_facts: true
  serial: 1
  roles:
    - preflight
    - platform
    - ryzenadj
    - fan
    - artifacts
    - service
    - verify
